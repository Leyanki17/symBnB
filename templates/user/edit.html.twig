{% extends 'base.html.twig'%}

{% block title %}Modification {% endblock %}

{% block body %}

    <div class="container mt-3">
           
            <h3 class="my-2">Modification de l'utilisateur {{ username }}</h3>
            <div class="text-left"> <a href="{{ path('edit_password_user')}}" class="btn btn-primary" >Modifier mot de passe</a></div>
            {{ form_start(form) }}

            {{ form_widget(form)}}

            <button class="btn btn-primary" type="submit">Modifier</button>
            

        {{ form_end(form)}}
    </div>

{% endblock %}